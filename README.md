# Проектная работа модуля 5. Поиск ошибок и оптимизация
## Отработка этапов отладки(debugging), профилирования и бенчмаркинга приложения broken_app

## Debugging

1) off-by-one в sum_even – устранён выход за границу;
2) Use-after-free в use_after_free – теперь значение копируется до освобождения Box, UB устранено;
3) Утечка в leak_buffer – больше не аллоцирует временный буфер;
4) Неверная логика в average_positive – фильтрация только положительных и корректное деление;
5) Нормализация split_whitespace() – покрывает все пробельные символы, что соответствуют тестам;
6) Исправление гонки данных в модуле concurrency - использование атомик счетчика, добавлен интеграционный тест для проверки `test_race_increment_correct_total()`;








## Profiling

1) Построение графика flamegraph для файла `demo.rs` для определения "узких" мест программы. Программа `demo.rs` была немного изменена в сторону увелечения времени выполнения, чтобы для достаточного количества данных стека для построения графа. Было определено, что основное время выполнения программы уходит на функцию slow_fib (~90%), а также на slow_dedup и normilize, файлы лежат в директории /broken_app/artifacts/reports/benchmarking/before
2) Также было проанализировано основное потребление памяти при выполнении `demo.rs`. Пиковое потребление: ~130 КБ, основная аллокация — заполнение вектора перед вызовом slow_dedup (demo.rs:25), файл /broken_app/artifacts/reports/benchmarking/before/massif_report_clean.txt.


## Benchmarking
1) 